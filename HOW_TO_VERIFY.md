# วิธีการตรวจสอบผ่าน Google Chrome (Verification Guide)

เนื่องจากระบบความปลอดภัยของ Browser (CORS Policy) อาจทำให้ไฟล์ `index.html` ไม่สามารถอ่าน `data.csv` ได้โดยอัตโนมัติเมื่อเปิดไฟล์โดยตรง ให้ทำตามขั้นตอนนี้:

## 1. การเปิดใช้งาน
*   **วิธีที่ 1 (แนะนำ):** ดับเบิ้ลคลิกไฟล์ `run_in_chrome.bat` ที่สร้างไว้ให้
*   **วิธีที่ 2:** คลิกขวาที่ `index.html` > Open with > Google Chrome

## 2. การโหลดข้อมูล
หากกราฟ **ไม่แสดงข้อมูล** ขึ้นมา:
1.  ดูที่มุมขวาบนของหน้าเว็บ
2.  กดปุ่ม **📂 Open CSV**
3.  เลือกไฟล์ `data.csv` ในโฟลเดอร์นี้

## 3. รายการที่ต้องตรวจสอบ (Checklist)

### ✅ A. การแสดงผลวันที่ (All View)
1.  กดปุ่ม **"All"** ที่มุมขวาบน
2.  ดูที่ **แกน X** (แนวนอน) ด้านล่าง
3.  **ผลลัพธ์ที่ถูกต้อง:** ต้องแสดงเป็น **วันที่** (เช่น `26/01/2026`) ไม่ใช่เวลา

### ✅ B. การ Drill-Down (เจาะดูข้อมูล)
1.  อยู่ในหน้ามุมมอง **All** หรือ **Week**
2.  นำเมาส์ไปชี้ที่เส้นกราฟ (เส้นไหนก็ได้) จนมี Tooltip ขึ้น
3.  **คลิก** 1 ครั้งที่จุดนั้น
4.  **ผลลัพธ์ที่ถูกต้อง:**
    *   กราฟจะซูมเข้าไปยังวันที่นั้นๆ (00:00 - 24:00)
    *   แกน X จะเปลี่ยนเป็นแสดง **เวลา** (รายชั่วโมง)

### ✅ C. การคำนวณค่าเฉลี่ย (Avg KPIs)
1.  สังเกตตัวเลขในกล่องด้านบน (Avg Export Power ฯลฯ)
2.  ลอง **ซูมเข้า** (โดยการคลิกแบบข้อ B หรือใช้เมาส์ลากถ้ามีฟีเจอร์นี้)
3.  **ผลลัพธ์ที่ถูกต้อง:** ตัวเลขค่าเฉลี่ยต้อง **เปลี่ยน** ไปตามช่วงข้อมูลที่เห็นในหน้าจอ ณ ขณะนั้น
